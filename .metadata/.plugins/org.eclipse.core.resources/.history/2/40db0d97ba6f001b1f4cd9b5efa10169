<html xmlns="http://www.w3.org/1999/xhtml"
	xmlns:h="http://java.sun.com/jsf/html"
	xmlns:f="http://java.sun.com/jsf/core"
	xmlns:p="http://primefaces.org/ui">

<h:head>
	<meta charset="utf-8" name="viewport"
		content="width=device-width, initial-scale=1"
		http-equiv="X-UA-Conpatible" />
	<title>Login</title>
</h:head>
<style type="text/css">
.ui-panelgrid.showcase-text-align-center .ui-grid-responsive .ui-panelgrid-cell
	{
	text-align: center;
}

.customPanelGrid .ui-panelgrid-content {
	align-items: center;
}

.card {
	height: 300px;
	margin: auto;
	position: absolute;
	top: 30%;
	left: 60%;
	width: 300px;
	display: flex;
	justify-content: center;
	align-items: center;
	background-color: #fff;
	box-shadow: inset 0 -3em 3em rgba(0, 0, 0, 0.1), 0 0 0 2px
		rgb(255, 255, 255), 0.3em 0.3em 1em rgba(0, 0, 0, 0.3);
	padding: 40px;
}

body {
	background-image:
		url("http://itectehuacan.edu.mx/TutoriasFinalSistema/Imagenes/backgrounds/1.jpg");
	background-color: #ccecfb;
	font: sans-serif;
}

button.ui-button {
	margin-top: 20px;
}

.p-shadow- {level
	
}
</style>
<h:body>
	<h2>
		<h:outputText value="Lista Ususarios"></h:outputText>
	</h2>

	<h:form>
    <div class="card">
        <p:dataTable value="#{usuarioBean.ObtenerUsuario()}" var="usuario">
            <p:column headerText="id">
                <h:outputText value="#{usuario.id}"/>
            </p:column>

            <p:column headerText="Nome">
                <h:outputText value="#{usuario.nome}"/>
            </p:column>

            <p:column headerText="Email">
                <h:outputText value="#{usuario.email}"/>
            </p:column>

            <p:column headerText="Senha">
                <h:outputText value="#{usuario.senha}"/>
            </p:column>
            
             <p:column headerText="Telefone">
                <h:outputText value="#{usuario.telefone}"/>
            </p:column>

            <p:column headerText="Editar">
                <p:commandButton action="#{usuarioBean.EditarUsuario(usuario.id)}" value="Editar"></p:commandButton>
            </p:column>

            <p:column headerText="Eliminar">
                <p:commandButton action="#{usuarioBean.eliminar(usuario.id)}" value="Eliminar"></p:commandButton>
            </p:column>
        </p:dataTable>
    </div>
    <p:commandButton action="#{usuarioBean.nuevo()}" value="Nuevo Usuario"></p:commandButton>
</h:form>
	
</h:body>
</html>